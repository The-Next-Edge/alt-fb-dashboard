        <div class="buckets">
            <% for (var i=0; i<buckets.length; i++) { %>
                <div class="droppable bucket ui-widget-header" data-name="<%= buckets[i].name %>">
                    <a href="/bucket/<%= buckets[i].name %>"><%= buckets[i].name %></a>
                    <p>drop here</p>
                </div>
            <% } %>
        </div>